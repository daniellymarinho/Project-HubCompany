import styled from "styled-components";

export const StyledItemsTech = styled.li`
  background: var(--Grey-4);
  border-radius: var(--radius);
  color: var(--Grey-0);
  display: flex;
  justify-content: space-between;
  width: 80%;
  padding: 10px;
  margin: 10px 0px;
  font-family: var(--font);

  .title{
    color: var(--Grey-0);
    font-size: 14px;
    font-weight: 700;
  }

.status{
  color: var(--Grey-1);
  font-size: 12px;

}
`;
